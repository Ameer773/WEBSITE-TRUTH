[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Optional: Setting Node.js version just in case Next.js 16 requires it
[build.environment]
  NODE_VERSION = "20"
