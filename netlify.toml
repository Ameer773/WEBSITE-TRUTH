[build]
  # Assuming the folder in your Git repo is exactly "Truth god" based on your local path
  command = "cd \"Truth god\" && npm install && npm run build"
  publish = "Truth god/.next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"

